interface:
  display_name: "Daily Vocab Word Page"
  short_description: "Prepare payloads and render quiet word pages"
  default_prompt: "Use $daily-vocab-word-page to gather word-page content and return a semantic JSON payload by default. Keep pronunciation compact as respelling + UK/US IPA, include CEFR and Zipf metadata, write original concept-first prose, and render files only when requested."
